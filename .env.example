# Job Radar does not require secrets for local scoring and digest generation.
#
# Archived outreach draft generation may use OpenAI, but those workflows are
# disabled by default and require explicit mutation flags.
OPENAI_API_KEY=

# Optional: export selected JD packages to the separate resume tailoring repo.
RESUME_TAILORING_REPO_DIR=C:\Users\solom\OneDrive\Documents\GitHub\resume-tailoring-workflow
RESUME_TAILORING_IMPORT_DIR=input
